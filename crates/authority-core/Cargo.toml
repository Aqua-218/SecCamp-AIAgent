[package]
name = "authority-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[lints]
workspace = true

[dev-dependencies]
proptest = "1.11.0"

[target.'cfg(loom)'.dependencies]
loom = "0.7.2"
